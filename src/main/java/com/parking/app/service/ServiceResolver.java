package com.parking.app.service;

//TODO to remove this , added factory map
public class ServiceResolver {


    /*public static ParkService resolveService(VehicleType vehicleType) {

        switch (vehicleType) {
            case CAR:
                return new CarParkServiceImpl();
            case MOTORCYCLE:
                return new MotorCycleParkServiceImpl();
            default:
                throw new IllegalArgumentException("VehicleDTO Type is unrecognised");
        }

    }
*/
}
